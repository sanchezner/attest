DATABASE_URL=your-database-url

NTFY_TOPIC=your-private-topic-name
# NTFY_SERVER=https://ntfy.sh

# worker tokens created via python -m cli.create_tokens --project <slug> --reporter <name>
# store raw tokens here locally; only hashes go in the DB
EXAMPLE_TOKEN_A=example-token-a
EXAMPLE_TOKEN_B=example-token-b